# DroneClear environment configuration
# Copy this file to .env and fill in your values.
# .env is gitignored and never committed.

# Which settings module to use: dev or prod
DJANGO_SETTINGS_MODULE=droneclear_backend.settings.dev

# Secret key — change this before deploying to production!
# Generate one with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=your-secret-key-here

# --- Production-only values (ignored in dev) ---
# DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
# CORS_ALLOWED_ORIGINS=https://yourdomain.com
